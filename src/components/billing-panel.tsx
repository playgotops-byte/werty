"use client";import {useState} from "react";import {authFetch} from "@/lib/client-session";const amounts=[250,500,1000,2500];export function BillingPanel(){const [amount,setAmount]=useState(500),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function pay(){setBusy(true);setError("");try{const r=await authFetch("/api/payments",{method:"POST",body:JSON.stringify({amount_rubles:amount,idempotency_key:crypto.randomUUID()})});const d=await r.json();if(!r.ok)throw new Error(d.error?.message||"Не удалось создать платёж");location.href=d.confirmation_url}catch(e){setError(e instanceof Error?e.message:"Ошибка")}finally{setBusy(false)}}return <><div className="dash-title"><div><div className="eyebrow">Balance</div><h1>Пополнение</h1></div></div><div className="billing-grid"><section className="panel"><div className="panel-label">Выберите сумму</div><div className="amounts">{amounts.map(x=><button key={x} className={"amount "+(amount===x?"selected":"")} onClick={()=>setAmount(x)}>{x.toLocaleString("ru-RU")} ₽<small style={{display:"block",color:"#718093"}}>{(x*20000).toLocaleString("ru-RU")} токенов</small></button>)}</div>{error&&<p className="form-error">{error}</p>}<button className="button primary" style={{width:"100%"}} onClick={pay} disabled={busy}>{busy?"Создаём платёж…":"Перейти к оплате"}</button></section><section className="panel"><div className="panel-label">Как проходит оплата</div><h2 style={{fontSize:24}}>Баланс зачисляется после подтверждения ЮKassa</h2><p style={{color:"#91a0b2"}}>Возврат на сайт не меняет баланс сам по себе. Werty проверяет платёж на сервере и защищает начисление от повторного webhook.</p></section></div></>}