"use client";import {useEffect} from "react";export function MotionController(){useEffect(()=>{if(matchMedia("(prefers-reduced-motion: reduce)").matches)return;const nodes=[...document.querySelectorAll<HTMLElement>(".reveal")];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("in-view");observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:"0px 0px -8%"});nodes.forEach(n=>observer.observe(n));let queued=false;const scroll=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{document.documentElement.style.setProperty("--scroll-y",String(scrollY));queued=false})};addEventListener("scroll",scroll,{passive:true});scroll();return()=>{observer.disconnect();removeEventListener("scroll",scroll)}},[]);return null}