"use client";import {useEffect,useState} from "react";import {authFetch} from "@/lib/client-session";type D={totals:{input:string;output:string;total:string;requests:number};models:{model:string;tokens:string}[]};const f=(v:string)=>Number(v).toLocaleString("ru-RU");export function UsagePanel(){const [d,setD]=useState<D|null>(null);useEffect(()=>{authFetch("/api/dashboard").then(r=>r.json()).then(setD).catch(()=>{})},[]);return <><div className="dash-title"><div><div className="eyebrow">Analytics</div><h1>Usage</h1></div><div className="button ghost">Последние 30 дней</div></div>{!d?<div className="loading">Загрузка…</div>:<div className="dash-grid"><div className="panel activity"><div className="stat-row"><div className="stat-cell"><div className="panel-label">Input tokens</div><strong>{f(d.totals.input)}</strong></div><div className="stat-cell"><div className="panel-label">Output tokens</div><strong>{f(d.totals.output)}</strong></div><div className="stat-cell"><div className="panel-label">Total tokens</div><strong>{f(d.totals.total)}</strong></div></div></div><section className="panel activity"><div className="panel-label">Usage by model</div>{d.models.length?<table className="table"><thead><tr><th>Model</th><th>Tokens</th></tr></thead><tbody>{d.models.map(x=><tr key={x.model}><td>{x.model}</td><td>{f(x.tokens)}</td></tr>)}</tbody></table>:<div className="empty">После первого API-запроса здесь появится статистика</div>}</section></div>}</>}