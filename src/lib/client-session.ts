export type StoredSession={access_token:string;refresh_token?:string;expires_at?:number};const KEY="werty_session";export function saveSession(session:StoredSession){sessionStorage.setItem(KEY,JSON.stringify(session))}export function getSession():StoredSession|null{try{const raw=sessionStorage.getItem(KEY);return raw?JSON.parse(raw):null}catch{return null}}export function clearSession(){sessionStorage.removeItem(KEY)}export async function authFetch(url:string,init:RequestInit={}){const s=getSession();if(!s?.access_token)throw new Error("Войдите в аккаунт");const h=new Headers(init.headers);h.set("Authorization","Bearer "+s.access_token);if(init.body&&!h.has("Content-Type"))h.set("Content-Type","application/json");const r=await fetch(url,{...init,headers:h});if(r.status===401){clearSession();throw new Error("Сессия истекла. Войдите снова.")}return r}