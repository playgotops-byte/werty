import {Suspense} from "react";import {AuthForm} from "@/components/auth-form";import {AuthLayout} from "@/components/auth-layout";export const metadata={title:"Вход"};export default function Page(){return <AuthLayout><Suspense><AuthForm mode="login"/></Suspense></AuthLayout>}