import {z} from "zod";import {authClient} from "@/lib/db";import {ApiError,body,guarded,json} from "@/lib/http";const schema=z.object({email:z.string().email().max(320),password:z.string().min(1).max(128)}).strict();export const POST=guarded(async request=>{const input=await body(request,schema,8192);const result=await authClient().auth.signInWithPassword(input);if(result.error||!result.data.session)throw new ApiError(401,"invalid_credentials","Неверный email или пароль.");return json({session:result.data.session})})
